spring:
  datasource:
    username: postgres
    password: "iadp$110"
    url: jdbc:postgresql://localhost:5432/uni2
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
   port: 8090

